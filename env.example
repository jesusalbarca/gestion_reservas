# Sample admin credentials for local testing
ADMIN_USER=admin
ADMIN_PASS=secret
# Uncomment to enforce auth outside production
# FORCE_ADMIN_AUTH=true

# SMTP configuration for email notifications
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false # usa true si el proveedor requiere TLS implicito (p.ej. puerto 465)
# SMTP_USER=apikey
# SMTP_PASS=secret
# MAIL_FROM="Reservas <no-reply@example.com>"

